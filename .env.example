# Backend (backend/server.js)
ANTHROPIC_API_KEY=
PORT=4000

# Frontend (leído por Vite al hacer build — ver src/api.js)
# En local puedes dejarlo vacío (cae a http://localhost:4000/api).
# En producción: la URL pública del backend + /api
VITE_API_URL=
